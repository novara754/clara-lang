opaque struct FILE;
extern fn c_string(s: string): ->c_char;
extern fn fopen(path: ->c_char, mode: ->c_char): ->FILE;
extern fn fclose(stream: ->FILE): c_int;

fn main() {
  let file = fopen(c_string("./Cargo.toml"), c_string("r"));
  fclose(file);
}
